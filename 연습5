#include <stdio.h>

int main(void)
{
	int fe;
	printf("값을 입력하세요. : ");
	scanf("%d", &fe);

	for (int i = 0; i < fe; i++)
	{
		for (int j = i; j < fe - 1; j++)
		{
			printf(" ");
		}
		for (int k = 0; k < i * 2 + 1 ; k++)
		{
			printf("*");		
		}
		printf("\n");
	}
	
	return 0;
}
